{
  "$id": "https://meta-mart.org/schema/security/secrets/securityConfiguration.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ValidateSSLClientConfig",
  "description": "Security configuration for the MetaMart server.",
  "type": "object",
  "javaType": "org.metamart.schema.security.SecurityConfiguration",
  "properties": {
    "maskPasswordsAPI": {
      "title": "Mask Password API",
      "description": "If enabled, it will mask all the password fields in the responses sent from the API except for the bots",
      "type": "boolean",
      "default": "false"
    }
  },
  "additionalProperties": false
}
